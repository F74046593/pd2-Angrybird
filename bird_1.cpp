#include "bird_1.h"

bird_1::bird_1(float x, float y, float radius, QTimer *timer, QPixmap pixmap, b2World *world, QGraphicsScene *scene) : bird(x, y, radius, timer, pixmap, world, scene){
}

void bird_1::skill(){

}
